import { StatusBar } from 'expo-status-bar';
import { Text, View } from 'react-native';

export default function App() {
  return (
    <View className=' flex items-center justify-center py-[500px]'>
      <Text className=' font-bold text-xl'>Eat my ass up App.js to start working on your app!</Text>
      <StatusBar style="auto" />
    </View>
  );
}
